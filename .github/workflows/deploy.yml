name: Deploy Praise Army (Songs + BibleMem + Knowledge + MIDI)

# One workflow, one artifact: the original songs page at "/", plus the same
# BibleMem/Knowledge/MIDI bundle deployed three times as top-level paths "/biblemem/",
# "/knowledge/", and "/midi/".
#
# The Drive audio index (web/public/data/bibleMemIndex.json) is checked
# into git like normal data, NOT regenerated on every push -- the crawl
# takes ~4 minutes (1,189 Drive folder listings) and the audio library
# barely changes, so re-running it on every CSS tweak was pure waste.
# Regenerate it manually: Actions tab -> "Run workflow" -> check
# "Refresh Bible audio index".

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      refresh_index:
        description: "Refresh Bible audio index (re-crawl Drive, ~4 min)"
        required: false
        type: boolean
        default: false

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Export Bible audio index from Drive
        if: github.event.inputs.refresh_index == 'true'
        working-directory: scripts
        env:
          FIREBASE_SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
        run: |
          npm install
          node export-bible-index.mjs

      - name: Commit refreshed index
        if: github.event.inputs.refresh_index == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add web/public/data/bibleMemIndex.json
          git diff --cached --quiet && echo "No changes to commit" || git commit -m "Refresh Bible audio index"
          git push

      - name: Install BibleMem dependencies
        working-directory: web
        run: npm ci

      - name: Build BibleMem
        working-directory: web
        env:
          VITE_DRIVE_API_KEY: ${{ secrets.GOOGLE_DRIVE_API_KEY }}
        run: npm run build

      - name: Assemble combined site
        run: |
          mkdir -p site
          cp index.html site/
          cp CNAME site/
          cp favicon.ico site/
          # Same bundle deployed three times -- App.tsx picks BibleMem, Knowledge, or MIDI
          # based on which directory served it (window.location.pathname).
          mkdir -p site/biblemem site/knowledge site/midi
          cp -r web/dist/. site/biblemem/
          cp -r web/dist/. site/knowledge/
          cp -r web/dist/. site/midi/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
